import actionCreator from "base-front-ts/actionCreator";
import { FORM_SUBMIT_START } from "./constantsForm";
const onSubmitStartForm = actionCreator(FORM_SUBMIT_START);
export default onSubmitStartForm;
